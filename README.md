# Weaviate Picture Search Example

This is an example code for a vector database in python using weaviate.

To start the database:

```shell
docker compose up
```

To run the code:

```shell
python ./Code/main.py
```